export enum Accesses {
  user,
  admin,
  task_editor
}

export enum Profession {
  none,
  pm,
  fronted,
  backend,
  design,
  tests
}

export enum Priorities {
  normal,
  serious,
  critical
}

export enum StatusesTask {
  canDo,
  develop,
  reopen,
  cancel,
  correct,
  check,
  finish
}
